{
  "name": "backend",
  "version": "0",
  "description": "Express + Zod API server. Registers handlers against the shared ApiSchema.",
  "dependsOn": ["shared"],
  "softDependsOn": ["db"],
  "ports": [{ "name": "BACKEND_PORT", "offset": 10 }],
  "workspace": { "path": "backend" },
  "devCommand": "yarn dev",
  "claudeFragment": "claude.md",
  "open": {
    "url": "http://127.0.0.1:${BACKEND_PORT}/health"
  }
}
